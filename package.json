{
  "name": "lean-flow",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "bun scripts/qa-verdict.ts sh scripts/qa-check.sh && bun test",
    "test:unit": "bun test",
    "gate": "bun scripts/qa-verdict.ts sh scripts/qa-check.sh",
    "conformance": "sh conformance.sh ."
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "^7.0.2"
  }
}
