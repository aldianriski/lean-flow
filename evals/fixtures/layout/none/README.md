# layout fixture — none

Intentionally empty: neither `TODO.md` nor `docs/work/` exists at this root. This file exists only
because git cannot track an empty directory; it is not itself part of the classified tree.
