Disk: four `*.md.template` files. Both halves of the claim agree.

The green control. Without it, a checker that FAILed unconditionally would satisfy every must-FAIL
case here and still look correct.
