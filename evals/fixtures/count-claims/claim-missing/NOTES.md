Disk: four `*.md.template` files.

A checker that treats "nothing to compare" as success passes here — which is how a claim that gets
deleted or reworded stops being guarded with nobody noticing. The file EXISTS, so this must FAIL.
A file that is absent entirely is a skip, and that difference is the point of this fixture.
