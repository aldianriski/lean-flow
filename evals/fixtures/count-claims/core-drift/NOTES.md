Disk: four `*.md.template` files, so core = four minus two non-core, and the total is four.

The mirror of total-drift: the total half agrees with disk and the core half does not. Both fixtures
are retained because one half passing must never stand in for the other being checked.
