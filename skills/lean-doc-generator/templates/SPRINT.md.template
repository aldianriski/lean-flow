---
sprint: NNN
slug: [kebab-slug]
stream: [stream name — omit entirely on a single-stream repo]
owner: [role — not personal name]
last_updated: YYYY-MM-DD
status: planned | active | closed
plan_commit: [sha — set at promote]
close_commit: [sha — set at close]
update_trigger: sprint execute/close events
---

# SPRINT-NNN — [Sprint Name] [CUSTOMIZE]

> **Theme:** [One–three sentences. What this sprint is really about and why it comes
> before other work. Foundations before features — don't build on sand.]

## Scope

**In:** [the 2–5 things this sprint will deliver]
**Out (deferred):** [explicitly named non-goals — what looks related but is NOT in this sprint]

## Plan

<!-- One block per task. Pull these from TODO.md Backlog at promote (dependency order). The DoD
     checkboxes are what /orchestrator sprint-bulk loops over and what /prime counts.
     Tasks sharing a file: fix single-owner + commit order in § Decisions (a D-row); at commit stage
     shared files per-hunk, never a plain `git add` over another task/stream's WIP (L-042/L-037).
     Mandatory per block: header meta carries `class:`
     (decision | execution | mechanical-ingest — advisory default, dispatch may override, ADR-010)
     + an autonomy tag (HITL|AFK); plus a `Depends-on:` line, `Layers:`, and `**Acceptance:**`. -->

### T1 — [verb-first title] `[size: M · risk: med · class: execution · HITL]` [CUSTOMIZE]
Layers: [files / modules this touches]
Depends-on: [Tn / TASK-NNN list, or none]
[1–3 sentences of WHY + the approach — never step-by-step HOW.]

**Acceptance:** [one observable, human-verifiable outcome — "done when …"]

**DoD:**
- [ ] [concrete, checkable step]
- [ ] [concrete, checkable step]
<!-- QA (suggestion, never a gate): does this task want tests (type → tdd/references/test-strategy.md),
     a lint / security-review pass, or a perf budget? Note it here; the Review step raises the same prompt. -->

### T2 — [verb-first title] `[size: S · risk: low · class: execution · AFK]` [CUSTOMIZE]
…

## Owner-action checklist
<!-- Non-dev actions a human must do (secrets, env, external dashboards). Not dev tasks. Omit if none. -->
- [ ] [e.g. rotate the exposed credential / set PROD env var]

## Decisions (pre-locked)
<!-- Hard-to-reverse + surprising + a real trade-off → also record as an ADR (docs/DECISIONS.md / docs/adr/). -->
- **D1** — [what was decided + why, one line]. **→ ADR-NNN** (if it qualifies).

## Assumptions
<!-- Each with how/where it gets confirmed. -->
- **A1** — [assumption]. *Confirm: [source].*

## Execution Log

> **Lives in its own file** — `docs/sprint/logs/SPRINT-NNN-<slug>.md`, rendered from
> `templates/sprint-log.md.template` and created lazily at the first entry. Append there, never here:
> the Log grows with the work done, so keeping it out of this file is what stops it consuming the
> 400-line budget the Plan needs (DOCS_Guide §9 · ADR-014). The `logs/` subdirectory is load-bearing —
> the sprint-file checks glob `docs/sprint/SPRINT-*.md` non-recursively, so a same-directory
> `-log.md` sibling would be capped and schema-checked as if it were a Plan.

## Files Changed
<!-- Filled during execution; feeds CHANGELOG at close. -->

| File | Task | Change (WHY) | Risk | Test |
|------|------|--------------|------|------|
| `path/to/file` | T1 | [why it changed] | Low | [how verified] |

## Retro
<!-- Written at close. Route the buckets to durable homes (DOCS_Guide §10):
     shipped → CHANGELOG.md (root; legacy docs/) · tech debt → TD-NNN · follow-ups → TASK-NNN · learnings → docs/LEARNINGS.md.
     After close, the file moves → docs/sprint/archive/ + a one-line entry in docs/sprint/INDEX.md (§11). -->

**Retrieval check** — did we fail to find, or contradict, a prior `L-NNN`/ADR this sprint? (yes → a Learnings-bucket friction + the retrieval-miss signal, DOCS_Guide §10)

**Cost** — what this sprint cost to run, and in what shape (inline · coordinator + N agents). Record it even when it is unremarkable: one row is an anecdote, and the series is what lets the next promote size a batch instead of guessing. Cost per unit **delivered**, not per unit attempted. Unavailable → say so rather than omitting the line.

**Worked**
- [what to keep doing]

**Friction**
- [what slowed us down]

**Pattern candidate** (surface to user → `docs/LEARNINGS.md`)
- [a confirmed learning worth promoting if it recurs — count ≥ 2 → durable rule]
