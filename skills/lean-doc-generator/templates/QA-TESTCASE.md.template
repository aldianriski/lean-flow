<!-- QA test-case — a repeatable check of ONE behaviour. NON-CORE template (like DESIGN):
     outside the core doc-generation loop. Filled instances live in docs/qa/ — one file per case,
     QA-NNN-<slug>.md; keep it small + agent-runnable. WHY/WHERE + observable outcome, never HOW. -->

# QA-[NNN] — [short case title] [CUSTOMIZE]

- **Area under test:** [one feature / command / behaviour — e.g. login rate-limit, `/task-decomposer` intake]
- **Preconditions / fixture:** [the throwaway repo state or input the case needs]
- **Last run:** YYYY-MM-DD — [pass | fail] [CUSTOMIZE]

## Steps
1. [action — what to invoke / do]
2. [action]

## Expected
[the observable artifact or outcome — e.g. "login locks after N failed attempts", "TODO gains TASK-NNN carrying `assumes:`"]

## Result
[pass | fail + what actually happened; on fail, link the `BUG` report / `TASK` / finding it routed to]
