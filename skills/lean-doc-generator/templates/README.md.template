<a id="readme-top"></a>

<!--
  README GUIDE — not a gate. Models a showcase-grade front-door; adapt freely — reorder,
  merge, drop, or add sections to fit the repo. Structure is a suggestion; two invariants
  only: the anti-SSOT rule below, and the footer ownership line at the bottom.

  Audience split: this file is the HUMAN showcase — the complete, readable, informative
  front-door a person lands on (promotion is legitimate here). Agent-facing context (project
  shape, vocabulary, anti-patterns) lives in CLAUDE.md / CONTEXT.md instead, not here.

  No hard line cap (DOCS_Guide §2 footnote) — this is the full front-door; keep every line
  signal-dense (LAW 4), not short for its own sake.

  ANTI-SSOT RULE: this README PRESENTS (prose, diagrams, summaries, links) — it never
  byte-duplicates a volatile table/list that already lives in CONTEXT.md or another SSOT doc.
  Summarize + link instead (ADR-007 pattern) if a section below tempts a table that will drift.

  Ownership stays a footer line, not top YAML frontmatter (DOCS_Guide §3 README exception),
  so the rendered page leads with the project, not a metadata block.
-->

<!-- HERO -->
<div align="center">

# [Project Name] <!-- CUSTOMIZE -->

**[One-sentence pitch — what it is + who it's for.]** <!-- CUSTOMIZE -->

<!-- Badges are optional — drop the row entirely if there's nothing worth signaling yet.
     Use shields.io (or your CI's own badge) inline — no reference-link footer needed. -->
[![License](https://img.shields.io/badge/license-[MIT]-green)](LICENSE) [![Version](https://img.shields.io/badge/version-[X.Y.Z]-blue)](CHANGELOG.md)

<!-- Optional logo/banner slot — delete if unused. -->
<!-- <img src="docs/assets/banner.png" alt="[Project Name] banner" width="600"> -->

[Quick start](#quick-start) · [Features](#features) · [Docs](#docs-map)

</div>

---

## Why [Project Name] <!-- CUSTOMIZE -->

<!-- 2-4 bullets on what makes this worth using. Promotion is legitimate here — this is the
     one section allowed to sell. -->
- [Reason 1 — the problem it solves]
- [Reason 2 — what makes the approach different]
- [Reason 3 — optional]

---

## Quick start

<!-- Copy-pasteable: install → first command. If there's a longer setup, link out — don't
     inline it here. -->
```bash
[install command]
[first command that produces visible output]
```

Full setup: [`docs/development/setup.md`](docs/development/setup.md)

---

## Features

<!-- Table or bullets — whichever reads better for this repo. Don't duplicate a living
     roster (e.g. a skill/module list already tracked in CONTEXT.md) — summarize + link. -->
| Feature | What it gives you |
|---|---|
| [Feature 1] | [one-line benefit] |
| [Feature 2] | [one-line benefit] |

---

## Architecture at a glance

<!-- Optional. A short diagram earns its place if the shape isn't obvious from Quick start.
     Delete this section if there's nothing worth diagramming yet. -->
```mermaid
graph LR
  A[Input] --> B[Core]
  B --> C[Output]
```

Full map: [`docs/architecture/overview.md`](docs/architecture/overview.md)

---

## Usage

<!-- The one workflow a new user runs first — not every command. -->
```bash
[the day-one workflow, step by step]
```

---

## Docs map

<!-- Links only — this section presents, never duplicates. -->
- [Architecture](docs/architecture/overview.md) — structure, boundaries
- [Changelog](CHANGELOG.md) — what shipped, per release
- [Decisions](docs/DECISIONS.md) — the WHY behind hard-to-reverse choices
- [Contributing](CONTRIBUTING.md) <!-- CUSTOMIZE — delete if not applicable -->

---

## License
[MIT — see [LICENSE](LICENSE).] <!-- CUSTOMIZE -->

<sub>Doc owner: [role] · last updated: YYYY-MM-DD · status: current · v[X.Y.Z]</sub>
<!-- footer ownership — not top frontmatter (DOCS_Guide §3 README exception); the one
     other invariant besides anti-SSOT above -->
