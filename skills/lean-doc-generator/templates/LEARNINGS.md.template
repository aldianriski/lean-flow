---
owner: [role — not personal name]
last_updated: YYYY-MM-DD
update_trigger: A learning confirmed at Sprint Close, or a learning promoted to a durable rule
status: current
---

# [Project Name] — Learnings Ledger

Append-only record of confirmed corrections and patterns surfaced at Sprint Close. A learning that
**recurs (count ≥ 2)** is promoted into a *durable* rule — a `CLAUDE.md` anti-pattern, a `CONTEXT.md`
rule, or a skill red-flag — and marked below. Reviewed at every **Sprint Promote** before planning.

<!-- Newest first. Never edit a past entry except to bump `seen` / `count`, set `promoted`, or
     add/extend `related:` — an optional cross-link to sibling learnings (`L-NNN (one-line why)`)
     that sharpens recall and surfaces recurring clusters, but is never required.
     Once an entry reaches promoted: yes, collapse it to a pointer line at the next promote (§11):
     "L-NNN → promoted: <where>" — the durable rule is the record from then on. -->

<!-- Per-entry metadata (schema, ADR-009): the heading carries `[tags: <tag>] [status: active|promoted|superseded]`
     (tags from the project's vocabulary). If the project generates a by-tag index, this file is the SSOT
     and the index is derived — regenerate after edits. -->

> **By-tag index** → generated from these entries if the project maintains one.

---

## L-001 [tags: <tag>] [status: active]: [one line — what we got wrong or confirmed, and the fix] [CUSTOMIZE]
- seen: Sprint-001
- count: 1
- promoted: no   <!-- once count ≥ 2: "yes → <CLAUDE.md anti-pattern | CONTEXT.md rule | skill red-flag>" -->
- related: —   <!-- optional: sibling learnings, e.g. L-007 (one-line why each); or — if none -->

---

<!-- Example (filled in) — a promoted learning: -->

## L-000 [tags: process] [status: promoted]: Unconfirmed auth assumptions cause regressions — confirm the auth model at G1
- seen: Sprint-003, Sprint-005
- count: 2
- promoted: yes → CLAUDE.md anti-pattern + task-decomposer assumption registry
- related: L-001 (the intake grill that surfaces the assumption before it regresses)
