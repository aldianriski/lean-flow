---
owner: Maintainer
last_updated: 2026-08-20
update_trigger: A sprint is closed and archived (DOCS_Guide §11)
status: current
---

# lean-flow — Sprint Index

One line per archived sprint (newest first); files live in [`archive/`](archive/).

- SPRINT-058 — Measure Before Moving — closed 2026-08-10 · debb320 (MINOR v1.32.0)
- SPRINT-057 — Prove the Guards — closed 2026-08-10 · 75c2f66 (MINOR v1.31.0)
- SPRINT-056 — Silent Passes — closed 2026-08-09 · 07637d6 (MINOR v1.30.0)
- SPRINT-055 — Wiring the Standard — closed 2026-08-09 · 9aa1075 (MINOR v1.29.0)
- SPRINT-054 — Rulings — closed 2026-08-09 · b31e170 (MINOR v1.28.0)
- SPRINT-053 — Surface Truth — closed 2026-08-09 · c6229c5 (PATCH v1.27.3)
- SPRINT-052 — Rule Placement — closed 2026-08-09 · 9f3da3d (PATCH v1.27.2)
- SPRINT-051 — Keeper Adoption — closed 2026-08-09 · 351252a (PATCH v1.27.1 — covers SPRINT-049 too)
- SPRINT-050 — Adoption Remainder — closed 2026-08-09 · b70ebb6 (docs-only, no release)
- SPRINT-049 — Layer-Check Redesign — closed 2026-08-09 · 75e61a8 (PATCH v1.27.1)
- SPRINT-048 — Epic Layer — closed 2026-08-09 · 3ff51d3 (MINOR v1.27.0)
- SPRINT-047 — Sprint Log Split — closed 2026-08-09 · 531e073 (MINOR v1.26.0)
- SPRINT-046 — Permission Surface — closed 2026-08-01 · e49cb55 (PATCH v1.25.2)
- SPRINT-045 — Gate Precision — closed 2026-08-01 · eaf3e1f (PATCH v1.25.1)
- SPRINT-044 — Night-Run Ergonomics — closed 2026-08-01 · a32090b (MINOR v1.25.0)
- SPRINT-043 — Proof Run — closed 2026-08-01 · 3bfae3c (released in MINOR v1.25.0)
- SPRINT-042 — Run to Finish — closed 2026-08-01 · cba78de (MINOR v1.24.0)
- SPRINT-041 — Debt Guards — closed 2026-08-01 · a90ab0e (no release — maintainer-only)
- SPRINT-040 — Freshness Row and Park Records — closed 2026-07-30 · ebef0fe (MINOR v1.23.0)
- SPRINT-039 — Eval Coverage and Release — closed 2026-07-30 · dfc60e4 (no release — maintainer-only)
- SPRINT-038 — Probes, Evals, and Layout — closed 2026-07-30 · e4d132c (MINOR v1.22.0)
- SPRINT-037 — Gates and Evals — closed 2026-07-30 · 08ca2a6 (MINOR v1.21.0)
- SPRINT-036 — Preflight and Verify — closed 2026-07-30 · ba46936 (MINOR v1.20.0)
- SPRINT-035 — Contract Hardening — closed 2026-07-30 · 7469411 (MINOR v1.19.0)
- SPRINT-034 — Night-Run Entry Path — closed 2026-07-30 · ce93d59 (MINOR v1.18.0)
- SPRINT-033 — Unattended-Run Contract — closed 2026-07-29 · 7eca438 (MINOR v1.17.0)
- SPRINT-032 — TemiDev Repo-Structure Standard — closed 2026-07-29 · 94da637 (MINOR v1.16.0)
- SPRINT-031 — Tech-Debt Split — closed 2026-07-29 · d4ab0c8 (MINOR v1.15.0)
- SPRINT-030 — Gate-Guard Decision — closed 2026-07-29 · 76ea8f3 (docs-only, no release)
- SPRINT-029 — Dispatch-Cost Note — closed 2026-07-29 · d886f61 (PATCH v1.14.1)
- SPRINT-028 — Research Delta-Scan Batch — closed 2026-07-29 · fc520e3 (docs-only, no release)
- SPRINT-027 — Night-Run Watchdog & Housekeeping — closed 2026-07-29 · 36721a4
- SPRINT-026 — Fleet & Night-Run Build — closed 2026-07-29 · 601e2e6
- SPRINT-025 — Fleet & Night-Run Foundations — closed 2026-07-29 · 2eb9bd5
- SPRINT-024 — Loop Hygiene & Wiring — closed 2026-07-17 · 296c01b
- SPRINT-023 — Dispatch & Parallelization — closed 2026-07-10 · 3d28333
- SPRINT-022 — Wiring Pass — closed 2026-07-10 · 5e827b2
- SPRINT-021 — Fog-Mode + Council Decorrelation Probe — closed 2026-07-10 · 8209c4d
- SPRINT-020 — Workflow Hardening — closed 2026-07-10 · 905be34
- SPRINT-019 — Model Tiers — closed 2026-07-10 · 8a20d1a
- SPRINT-018 — Cleanup — closed 2026-07-10 · 719d0fa
- SPRINT-017 — Doc-Gen Features — closed 2026-07-10 · 984b9eb
- SPRINT-016 — Research Spikes — closed 2026-07-10 · 548cc6f
- SPRINT-015 — Loop Hardening — closed 2026-07-10 · bce0087
- SPRINT-014 — Knowledge Corpus + Council Hardening — closed 2026-07-02 · c02dbf1
- SPRINT-013 — Knowledge Metadata — closed 2026-07-02 · 6051bf6
- SPRINT-012 — Process Hardening — closed 2026-07-02 · bb39c7f
- SPRINT-011 — Audit fixes — closed 2026-06-22 · 8029677
- SPRINT-010 — Audits — closed 2026-06-22 · bf8bd5b
- SPRINT-009 — External face + SSOT diet — closed 2026-06-21 · 74fa6f6
- SPRINT-008 — QA discipline — closed 2026-06-21 · 7f27438
- SPRINT-007 — Extend & Harden — closed 2026-06-16 · d6025af
- SPRINT-006 — Review Depth & Context Diet — closed 2026-06-12 · 656ac3c
- SPRINT-005 — Conform & Validate — closed 2026-06-12 · 185b749
- SPRINT-004 — Token & Doc Hardening — closed 2026-06-12 · dc839b1
- SPRINT-003 — Validate & Harden — closed 2026-06-11 · 40bf188
- SPRINT-002 — Dogfood Fixes — closed 2026-06-11 · 007869e
- SPRINT-001 — Ship & Validate — closed 2026-06-11 · b0f2695
- SPRINT-059 — Prove the Run Finished — closed 2026-08-10 · 0b4e06a
- SPRINT-060 — Make Room — closed 2026-08-10 · afd693d · 4 of 5 units (T5 carried, acceptance unmet)
- SPRINT-061 — Named, Not Answered — closed 2026-08-10 · 2f90504 · 3 of 3 units (v1.35.0)
- SPRINT-062 — Room to Write — closed 2026-08-10 · f0f72c0 · 3 of 3 units (v1.36.0) · EPIC-002
- SPRINT-063 — Headroom — closed 2026-08-14 · 3998e23 · 4 of 4 units (v1.37.0) · EPIC-002
- SPRINT-064 — Where It Fires — closed 2026-08-14 · 92a16c9 · 3 of 3 units (v1.38.0) · EPIC-002
- SPRINT-065 — The Critic Loop — closed 2026-08-15 · c723b76 · 3 of 3 units (v1.39.0) · EPIC-002 (closes the epic)
- SPRINT-066 — Verification Authority — closed 2026-08-15 · 029f698 · 2 of 2 units (v1.40.0) · ADR-021 + ADR-022
- SPRINT-067 — The Proof Layer — closed 2026-08-15 · 7df2d56 · 2 of 2 units (v1.41.0) · system-verify + per-criterion evidence
- SPRINT-068 — Open the Standard — closed 2026-08-15 · 9fef02d · 3 of 3 units (v1.42.0) · ADR-023 opens EPIC-003
- SPRINT-069 — First Extraction — closed 2026-08-16 · b744fed · 5 of 5 units (v1.43.0) · EPIC-003 (spec extracted · ADR-024)
- SPRINT-070 — Attested — closed 2026-08-16 · d164924 · 10 of 10 DoD (v1.44.0) · EPIC-003 (spec §13 attestation · ADR-025) · TD-054 resolved
- SPRINT-071 — Cite, Not Restate — closed 2026-08-16 · 015d663 · 14 of 14 DoD (v1.45.0) · EPIC-003 CLOSED (skills cite not restate · spec 0.3.0 makes Gated checkable) · TD-060 filed
- SPRINT-072 — Conformance Baseline — closed 2026-08-16 · 87954f2 · 17 of 17 DoD (v1.46.0) · EPIC-004 (all 96 spec rules classified · the checkers do not check the standard) · TD-061 filed
- SPRINT-073 — The Spec as Rule Source — closed 2026-08-16 · e7ce99b · 15 of 15 DoD (v1.47.0 · spec 0.4.0) · EPIC-004 (the spec becomes the rule source · ADR-026) · TD-058 resolved · TD-062 filed
- SPRINT-074 — The First Spec-Driven Checker — closed 2026-08-18 · 6016738 · 15 of 15 DoD (v1.48.0 · spec 0.4.1) · EPIC-004 (first checker driven by the spec, not hard-coded · §13 attestation covered · Closed-when 4 ticked) · TD-037 resolved · TD-063 filed
- SPRINT-075 — The Conformance Engine — closed 2026-08-20 · 2edc606 · 26 of 26 DoD (v1.49.0 · spec unchanged at 0.4.1) · EPIC-004 (one checker becomes the engine · engine gaps stop counting against the repo under test · ADR-027 · Closed-when 1 + 5 ticked) · TD-064/065/066 filed
- SPRINT-076 — Coverage, and Whether the Bar Is Right — closed 2026-08-21 · 74839bd · 20 of 20 DoD (v1.50.0 · spec 0.4.2) · EPIC-004 (§4 ADR family + §2 placement pair, coverage 6 → 13 of 62 · §3's two exceptions written down · Closed-when 2 RULED "bar stands", Closed-when 3 established not ticked) · TD-067/068 filed · TD-064 halved
- SPRINT-077 — The Decisions EPIC-004 Is Waiting On — closed 2026-08-21 · 5115885 · 9 of 9 DoD (v1.51.0 · spec 0.5.0) · EPIC-004 (**Closed-when 3 TICKED** on two rulings — invocation errors out of scope so identities read 16 of 16; condition adopts the wider property, prior wording preserved. Epic to **4 of 5**) · §2's four loop rows name their substrate instead of `always` — artefacts 4 of 8 → **0 of 4**, with no code edit · TD-070 filed (three §2 parsers) · TD-069 updated · L-146 (a retained fixture decayed to a vacuous pass) · TASK-245 filed
- SPRINT-078 — The Checks a Stranger Cannot See — closed 2026-08-23 · a7b5ffc · 15 of 15 DoD (v1.52.0 · spec unchanged at 0.5.0) · EPIC-004 (coverage **19 → 30 of 62**, the epic's largest move · §13's five attestation rules **migrated off `check-attestation.sh`, deleted** — they had worked since SPRINT-074 and no adopter could reach one · §2/§6's tier family as one check with the tier **declared** in a new `.conformance-tier`, since §6 marks detection judged · §2's README footer read from §3's own example. Closed-when 2 stays open at 30 of 62) · **the migrated check caught its own shipping commit** (a `Gate:` line in the message parsed as a §13 trailer) · `hold`, a fourth verdict class, so the level ladder cannot certify Attested over an unsigned attestation · TD-071/072 filed, TD-070 extended 3 → 5 parsers, TD-065 resolved · L-147/148/149/150 filed, L-144 → count 3 · TASK-255/256 filed (two §2 gaps the engine reported rather than guessed)
- SPRINT-079 — The Undifferentiated Middle — closed 2026-08-23 · 634e4b9 · 34 of 34 DoD (v1.53.0 · spec 0.5.0 → 0.8.0) · EPIC-004 (**§ Closed-when 2's residual removed**: the eleven `scope-out` rules were correctly classified in a place the engine cannot read, so all eleven reported to every adopter as `rule-unimplemented`; §14 gains `restated` ×7 and `standard-directed` ×4 — **ADR-028**, checkable 62 → 51, classification unchanged at 100. Coverage **24 → 33**: §9's sprint-file family and §10's learning-governance family. §2 addressable where §6 points — Multi-service's rows + the rank-4 mapping, `DECISIONS.md` a literal path substrate-gated. Every finding now names its rule, appended not prefixed because a prefix would have vacuously satisfied four guards asserting a finding's *absence*. Condition stays open at **39 of 51**; the remaining 12 are TASK-250/251/252 exactly) · T6 added by amendment · TD-073/074 filed, TD-069/070/071 moved · L-151/152/153/154, L-108 → count 10 · **three defects found by running the new checks on this repository**: two §9 rules contradicting each other on this sprint's own Plan, an octal abort on `sprint: 079`, and a harness shipped unwired that the gate caught at close
- SPRINT-080 — The Last Twelve Rules — closed 2026-08-23 · 309c0de · 35 of 35 DoD (v1.54.0 · spec 0.8.0, §11 gains the LEARNINGS deliberate-non-collapse clause) · **EPIC-004 CLOSED** — its ninth and final member. § Closed-when 2 ticked with **no amendment**: 51 of 51 rules map to a check or carry an explicit non-evaluated mark (51 checkable + 49 marked = 100), and the register's § `build` bucket is **empty**. Engine coverage **33 → 45** in-engine, 6 more by standalone checkers. **Twelve rules shipped** — §11 ledger retention (`S11.TDDELETE`/`TODOCAP`/`LEARNINGS`/`BACKLOG`), §11 archival (`S11.SPRINT` ×2 findings/`LOGPAIR`/`CHANGELOG`/`WHENITRUNS`), §12 git boundary (`S12.SECRETS`/`BACKUPS`/`DESIGNSRC`/`GENERATED`) — every threshold **read from the spec**, proven by flipping §11's retention delay in a scratch spec and watching the same repo change verdict with no code edit. §12's four require **two signals that agree**; size thresholds refused on the record because §12 states no number, and six benign lookalikes were built **before** any detector · **four defects fixed in checks this repo already shipped**: `S9.VERIFYCLAUSE` fired on every sprint between promote and its first tick (empty command substitution in a heredoc yields one empty line), `check-epic-archive.sh` read `member_sprints` **zero times** so it enforced half of what §11 calls a two-part test, root `CHANGELOG.md` had 38 rotated files and **no link line**, and TD-073's stated cause was wrong — the engine's runtime was its own per-rule bookkeeping, not the shipped spec (harness **9m24s → 3m20s** while growing 38 → 67 cases) · **six defects found by running each new rule against this repository before writing its fixture** (D2), every one in the check rather than the tree · retained fixtures **24 → 67** · TD-073 resolved, TD-075/076 filed · L-155/156/157 filed
- SPRINT-081 — Clean Slate — closed 2026-08-24 · 509f7f0 · 25 of 25 DoD (v1.55.0 · spec 0.8.0 → 0.9.0) · **the repo reaches its own standard's Structural bar: `level: none` → `Gated`**, the remaining gap being commit signing, which §13 states is the only route to Attested. T1 wrote the **sixteen ownership headers** (3 `docs/qa/` + 13 `docs/research/`), clearing `S1.LAW3` and `S3.SCHEMA` — all 222 docs pass both; counts re-derived before writing (3 + 13 = 16) and reconciled a fourth way afterwards (206 + 16 = 222). T2 shipped **`.conformance-exempt`**, a third declared file letting a repository rule a tier doc unnecessary *with a mandatory reason* — a bare path fires `exemption-reason-missing` and the doc **stays owed**, every accepted exemption is printed back with its reason, and the path is matched whole so `docs/` cannot exempt a tree (**ADR-031**); the rejected arm (condition-gate §6's Base rows) was refused **on a measurement** — seeded into a scratch spec it silences both findings for a repo declaring nothing and the tier reports *"no unconditional doc is owed at Base"*. **T4 was added mid-sprint by owner ruling** after T2's fix made a latent defect live: the level ladder held a repo at Gated for an attestation *claimed and unsigned* but nowhere at all when **none was claimed**, so claiming nothing outranked claiming honestly and any adopter with zero findings was certified `Attested` — now a named `attestation-absent` hold, never a failure, exit code unmoved (TD-079). T3 re-ran the **foreign-repo artefact triage at 45 rules** (EPIC-004 called its own result *honest but early* at 6): **9 findings, 9 actionable, 0 artefacts**, reconciled three ways, with Round 4 written to `docs/research/logs/` because the parent sat at 129 of 130 lines; its keeper finding is that the two `S6.BASE` rows a stranger sees are exactly the pair T2 made **answerable by a decision rather than only by a document** — the one thing dogfooding could not check (L-016) · TD-078/TD-080 filed, TD-064/077/079 resolved · TASK-259 filed · **L-158** (a fixture that asserts a finding's *wording* is not asserting its *consequence* — the case that should have caught T4's bug passed for its whole life, proven by a seeded break that reddened three new cases and left it green) · **L-159** (a defect can hide in a branch your own repository has never been in a state to reach) · L-136 bumped to a third artifact kind, a checker's *scope* · Retro records that nearly every gate failure this sprint was in the Plan's description of its own verification, not in the work
- SPRINT-082 — Foundation Hardening — closed 2026-08-24 · e39473e · 38 of 38 DoD (v1.56.0) · **standalone, no `epic:` stamp** — the proof-boundary sprint that precedes the fleet standard, on the reasoning that a boundary leaking in one repository leaks in N. **Three corrections, one shape: absence of evidence had been reading as evidence of absence.** T1 made `no-gate-discovered` route on the change's **risk class** instead of always continuing to close, and gave discovery a **fourth rung** — `.gate-command`, a declared file, ranked *last* because a declaration written *for* the pass is the weakest evidence (**ADR-033**); without it this repository would have parked its own runs, since all three existing rungs miss here. T2 replaced the Review skip table's `docs / config / trivial` row — an exemption handed out by **file extension**, so the changes least examined were sometimes the ones governing everything else — with routing on **behaviour impact + governance impact**, consuming T1's classifier rather than minting a second definition of risk. T3 put a **verification-reachability test** into G2: EXISTS · RUNS · **REACHES** · PROVES, after SPRINT-081 T1 froze a `Verify:` naming three `docs/qa/` files its checker structurally could not examine — it ran `66 PASS, 0 FAIL` and said nothing about its subject. T4 dogfooded all three end-to-end and **T2's correction fired by refusing**, declining to let a governance change self-certify. T5 wrote the execution-architecture **freeze** into `adlc-epic-sequencing.md` — the file epic-admission actually reads (L-151) — after a §11 compaction made room · TD-081 filed (`qa-check` prints two verdicts and only one is the tally, so `0 fail` does not mean the conformance leg found nothing) · **L-160** (a criterion foreclosed by a *repair* an earlier task in the same Plan makes — L-111 with the polarity reversed) · **L-161** (a whole-file `sed` on a DoD line that repeats across tasks ticked three tasks at once, each citing a real gate run that was not theirs — caught by arithmetic, not by review, because a prematurely ticked box looks exactly like a satisfied one) · **the sprint's own rules bit its authors repeatedly and correctly**: T2's routing forbade T1–T3 from self-review, T3's checker found T2's Verify clause naming an unreachable target, and every catch came from machinery or arithmetic rather than recall — L-136, L-156, L-158, L-108, L-058 and L-142 were all retrieved and applied *before* the work and four fired anyway · T5's review parked (`governance:high`, no independent reviewer) → **TASK-266**
- SPRINT-083 — TS/Bun Foundation — closed 2026-08-24 · 3332857 · 26 of 26 DoD (v1.57.0 · spec unchanged at 0.10.0) · **EPIC-014's first member sprint**, and the first executable code lean-flow has outside `scripts/`. **T1 froze the comparand before anything could be measured against it** — a migration that decides what "the same answer" means *afterwards* is a rewrite calling itself parity (**ADR-034**). The rule-ID denominator was **derived, not inherited**: three numbers were circulating and all three are now reconciled — **100** is the contract's (`51 + 49`), **51** is *checkable* (`mechanical 40 + split 11`, agreeing independently with EPIC-004's `45 in-engine + 6 standalone`), and **79 is a disproved query** whose pattern stopped at a hyphen and missed exactly the 21 hyphenated §2 ids (`79 + 21 = 100`, zero false positives). **The Finding-ID half could not be frozen and is recorded as a named gap** — the Shell engine emits findings through 4+ message shapes as free text, and three extractions returned 4 / 14 / 78; that is the sharpest argument for the migration this epic has, and it closes at H07/H08. T2 stood up the workspace with **zero dependencies** (Bun runs TS directly, so a consumer's cache gains inert files and no toolchain — **ADR-035**), and its manifest is this repository's **first rung-1 gate-discovery hit**, silently outranking the `.gate-command` ADR-033 added one sprint earlier — safe *only* because `scripts.test` invokes the real gate, now guarded by a retained must-FAIL fixture. T3 made the Clean-Architecture direction five mechanical rules over six fixture trees; T4 typed the Standard's vocabulary at **six** marks, not V3's four (`restated` and `standard-directed` were missing — the two ADR-028 added to stop eleven rules reporting as *"unchecked gaps someone can close"*) · **ADR-036** supersedes ADR-034's Severity row, which had frozen a target-state vocabulary the engine does not have, and states the rule the defect revealed: *every row of a compatibility contract must point at an artifact the current system has* · **every one of the three dispatched reviewers found something and two found blockers** — a substring match that reported *not bypassed* on a gate command merely printed inside an `echo`, and a scanner that missed `require()` entirely and lost any import wider than 200 characters; both are false NEGATIVES, and both suites were green · **the reviewer's own seeded break found a state-machine branch with zero coverage that three of my seeds had missed** (→ L-164) · T4 broke T3's guard on real code within minutes of it shipping, and the guard was wrong, not the code · TD-082/083/084 filed, TD-073 deleted on its §11 clock · TASK-271/272 filed · **L-162** (a frozen criterion can carry its own antidote) · **L-163** (one control character reclassifies a source file as binary — it fired twice, the second time inside its own entry) · **L-164** · sightings on L-108 ×3, L-045 ×2, L-137, L-130, L-100 · §11 retention took conformance from **6 FAIL → 0** and `level: none` → **Gated** · closed on an **owner ruling over partial gate evidence**, because `qa-check.sh` could not complete in three attempts (TD-084)
- SPRINT-084 — Gate Recovery and Owed Work — closed 2026-08-25 · 9ed3fae · 19 of 19 DoD (v1.57.1 · spec unchanged at 0.10.0) · **standalone, no `epic:` stamp** — the instrument sprint: SPRINT-083 had to close four DoD on an owner ruling because `qa-check.sh` could not print a verdict line, so nothing this repository built could be *verified*. **T1 restored it: ~900s never completing → 492s and `QA-CHECK: 176 pass, 3 fail`**, the first system-verify since SPRINT-083 to produce a verdict at all. The cure came from **profiling before fixing, exactly as TD-084 demanded** (*"do not act on (a) before (b)"*) — and the measurement overturned the assumption everyone held: the dominant term is **process-spawn count on this host**, not corpus size or check count. A **tiny-input isolation** (each spawn type timed 100× against a 1-line file, *before* any fix was chosen) put the bare process-creation floor at **21.1ms** against 110–260ms at real scale — the floor proves the mechanism with workload subtracted out, the real figure prices it. `gen-index.sh` alone was **523 spawns**; leg 4 went **271.5s → 23.6s** and leg 2f-ter **176.6s → 1.9s**, with **nothing deleted** and every heavy leg still reachable under `QA_FULL=1`. TD-073's lesson held a second time; a split chosen before measuring would have optimised the wrong half. Recorded as **§ Round 4** of `docs/research/logs/qa-gate-timing.md` because a measurement living only in a transcript is not a record (L-151) — and the round names leg 12 (eval harnesses, 396.3s, ~81%) as the *new* dominant term rather than recording only the win. New Tier G guard `qa-budget-check.sh` + its retained fixture: an over-budget run is **reported with its skipped harnesses named** instead of dying past an external timeout with no verdict — TD-084's failure mode closed at the root, discrimination-proven (seed landed by `cmp`, parsed by `sh -n`, 38=38 lines, case red while both sibling controls stayed green), and a genuine **flaky assertion was found and fixed while building it** (an exact-string match tipping across a wall-clock second, visible as the gate verdict flipping 175/176 between identical runs) · **T2 discharged TASK-266, the review SPRINT-082 correctly parked** — `governance:high` cannot take the self-review floor, so writing `self-review` would have been false *and* would have reddened the checker T2 itself had added. **Four independent scoped reviewers, one per task, all pinned to shipped refs** because T1 was concurrently editing `qa-check.sh` — **and four for four returned findings**: `check-system-verify-block.sh` satisfies a later unresolved FAIL with an *earlier* entry's ruling (whole-file greps, no positional link; reproduced `PASS` exit 0 in both orderings) and is **never live-wired** into `qa-check.sh` while its sibling from the same commit is; `check-review-depth.sh` grades only lines that *exist*, so `governance:high` work with **no** `review ·` line exits 0 — which is precisely what SPRINT-082 did, closing 38 of 38 with zero lines on the record; `check-verify-reaches.sh` certifies a script that **excludes** the claimed path (`-prune`) and collides on prefixes (`src/db` ↔ `src/dbtools/`), both cases its own Acceptance named as the thing to catch; and SPRINT-082's **execution-architecture freeze is unreachable by the epic-creation flow that must obey it** (L-151, in the sprint that promoted its neighbours). The four `review ·` lines went to the archive log where SPRINT-082's record lives, with the DoD's named verify recorded **not-applicable rather than vacuously passed** — `check-review-depth.sh` skips `*/archive/*`, demonstrated by running it against the file and watching it print nothing and exit 0 · **T3** realigned ADR-034/036 to `ADR.md.template` with § Decision **byte-identical** in both (extracted and compared directly, not read off a diff summary), reclassified `governance:high` against its own `risk: low` because `S4.SECTIONS`/`S4.NEGATIVE` mechanically parse ADR structure and ADR-034 is itself a contract — the "it's only docs" case T2's rule exists to catch; independent review **clean**, 4→4 Alternatives rows, no fusion (L-009) · **T4** exercised the absent-attestation hold SPRINT-081 T3 could not reach, adding a **second** foreign target with real git history rather than mutating the git-less stranger (whose existing invariants would have silently changed meaning): `attestation-absent` · `level: Gated` · **exit code unmoved, asserted A/B on the byte-identical tree** — `0` before history existed (§13 `not evaluated`, falling through to a false `Attested`) and `0` after, which pins the **L-159 bug shape** as a retained fixture rather than merely asserting a number; consumer-path coverage, the one thing dogfooding structurally cannot check here (L-016) · **T5** ruled `05`'s four candidates on the **delta over existing surface** (L-017) — and **the review changed a ruling**: the draft rejected Candidate D claiming all six experiment classes were already shipped with "no unmatched remainder", where only **2 of 6** are, moving D reject → defer gated on `05`'s own prescribed measurement; it also carried a **stale `11 check-*.sh`** copied from a SPRINT-077 doc when disk has **12**, and a negative claim that omitted `L-044` · **the sprint's own gate found FAILs true for sprints and invisible only because the run never reached that leg** — three at system-verify, one of them self-inflicted by the coordinator's `verify n/a` annotation (`n/a` contains a slash, so the path extractor read it as a claimed target), one a race against in-flight T4, and **two ruled rather than edited away**: T5's DoD names two scripts in one `Verify:` clause the checker pairs against each other, and rewriting a frozen criterion so a gate turns green is L-088 whatever the checker suggests · TD-085..TD-090 filed, **TD-084 resolved** → TASK-272 · TASK-273/274 filed `origin: close-retro` · **L-165** (six guard defects, one shape, **zero caught by recall** — every one found by an independent pass or a disagreeing second number, with the governing rules loaded and on screen; L-155's thesis observed six further times, arguing a standing budget for the outside pass rather than another sentence) · **L-120 bumped to a sixth sighting, in the coordinator's own hands**: a gate verification printed *"gate finished, output captured"* at exit 0 for a run where `$TMPDIR` was unset, the redirect died on `Permission denied` and **the gate never executed** — caught only by going to look for the artifact · the stale id-policy pointer corrected `L-159 → L-165`, the exact drift that issues a duplicate id (L-143) · **D4 executed**: TASK-266/259/260/271/272 deleted from the Backlog outright, the step SPRINT-083 missed and this promote had to clean first (TODO.md 202 → 135 lines) · Retro records the cost series' first entry — coordinator + **11 dispatched agents**, ≈1.92M subagent tokens, **≈383k per task delivered**, with the review half (~45% of spend) the only component that found anything the builders had missed
